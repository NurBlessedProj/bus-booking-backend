# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/bus_booking_app
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bus_booking_app?retryWrites=true&w=majority&appName=Cluster
# Example Atlas connection (add database name before the ?):
# MONGODB_URI=mongodb+srv://blessednur67:HOODQUAN67@cluster0.wftw5hq.mongodb.net/bus_booking_app?retryWrites=true&w=majority&appName=Cluster

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here_change_in_production
JWT_EXPIRE=7d

# Email Configuration (Resend)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key_here

# FROM Email Address - This is the sender email that appears in all emails
# 
# OPTION 1: For Testing/Development (No domain verification needed)
# Use Resend's test domain:
RESEND_FROM_EMAIL=Bus Booking App <onboarding@resend.dev>
#
# OPTION 2: For Production (Requires domain verification)
# Use your own verified domain:
# RESEND_FROM_EMAIL=Bus Booking App <noreply@yourdomain.com>
# OR just:
# RESEND_FROM_EMAIL=noreply@yourdomain.com
#
# Format examples:
# - "App Name <email@domain.com>" (with display name)
# - "email@domain.com" (just email, no display name)
#
# To verify your domain:
# 1. Go to https://resend.com/domains
# 2. Add your domain
# 3. Add the DNS records provided by Resend
# 4. Wait for verification (usually a few minutes)

# App Configuration
APP_NAME=Bus Booking App
APP_URL=http://localhost:3000

# Frontend URL (Optional - for mobile app, leave as * or omit)
# For web apps: http://localhost:3000 or your web URL
# For mobile apps: * (allows all origins, mobile apps don't have CORS restrictions)
# For email deep links: busbookingapp:// (mobile app scheme)
FRONTEND_URL=*
